
{% block body %}
    <h2>Article creation</h2>
    {{ form_start(form, {'action': path('article_new')}) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}
{% endblock %}
