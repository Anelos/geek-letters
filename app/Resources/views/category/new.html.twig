
{% block body %}
    <h1>Category creation</h1>

    {{ form_start(form, {'action': path('category_new'), 'method': 'POST'}) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

{% endblock %}
